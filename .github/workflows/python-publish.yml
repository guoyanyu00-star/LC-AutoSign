name: JLC Auto Sign

on:
  schedule:
    - cron: '0 0 * * *'  # UTC 0点 = 北京时间 8点，启动后随机延迟1小时内
  workflow_dispatch:

:
:
: 
:
      # 强制启用 Node.js 24，彻底消除警告
: 
:
      - : 
: actions/checkout@v4  # 已兼容 Node24，无需升级

      - : 
: actions/setup-python@v5  # 已兼容 Node24，无需升级
:
: 

      - : 安装依赖
: |



      - : 随机延迟 0~60分钟 签到
:
: ${{}}
: ${{}}
: |

